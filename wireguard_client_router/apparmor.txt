#include <tunables/global>

profile wireguard_client_router flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  file,
  capability net_admin,
  capability net_raw,
  network,

  signal (send) set=(kill,term,int,hup,cont),
  signal (receive),

  # S6-Overlay from the Home Assistant base image
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  /run.sh rix,
  /bin/bash rix,
  /usr/bin/python3* rix,
  /usr/bin/wg rix,
  /usr/bin/wireguard-go rix,
  /sbin/ip rix,
  /usr/sbin/nft rix,

  /app/** r,
  /data/** rwk,
  /config/** rwk,
  /tmp/** rwk,

  /dev/net/tun rw,
  /proc/net/** r,
  /proc/sys/net/ipv4/ip_forward rw,
  /sys/class/net/** r,
}
